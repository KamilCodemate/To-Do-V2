import React, { useState, useEffect, useCallback, FormEvent } from 'react';

const UserPanel: React.FC<{}> = (): React.ReactElement => {
  return <div className='user-panel'></div>;
};

export default UserPanel;
